"""FRAME-track training-data generation for the FOCUS submission.

`kb`      — parse the published QA into per-frame facts (the frame knowledge base)
`derive`  — emit new questions whose answers the knowledge base entails
"""
